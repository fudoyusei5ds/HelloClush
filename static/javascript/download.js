window.onload = () => {
    document.querySelector("#queue-input").addEventListener("keyup", (evt) => {
        let query_text = evt.target.value
        if (query_text) {
            
        }
    })
}